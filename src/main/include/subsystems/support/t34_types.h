#pragma once

// Enumeration used to drive orientation
enum class SwerveDriveMode
{
	RobotCentric,
	FieldOriented
};

enum class SwerveModulePosition
{
    LeftForward,
    LeftAft,
    RightForward,
    RightAft
};